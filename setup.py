from setuptools import setup, find_packages

setup(
    name='reassigned_spectrogram',
    version='0.1.0',
    packages=find_packages(include=['reassigned_spectrogram', 'reassigned_spectrogram.*'])
)
